program Test4;
var
   A, B, R, I: integer;
   t : boolean;
procedure teste (A:integer; B:real); 
var
   X: real;
   S: boolean;
   procedure imc (peso:integer; altura:real);
     var
        p,a: real;
     begin
        S := A + R * X;
     end
begin
   imc(X, A);
end  {verifique se é necessário um ";" no fechamento de um procedimento}

procedure calculaPeso(massa:real; gravidade:real);
var
   asd, c: real;
begin
   
end  

begin
   while (A >= 10) do
   begin
      A := A+1 + 3 * (4 + 4);
      B := R + 10;
      R := A + I;
      I := I + 1;
      teste(B, B);
   end
end.

{retirar algumas palavras reservadas para gerar erros sintáticos}